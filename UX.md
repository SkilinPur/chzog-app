# ЧЗОГ — приложение: как пользоваться (детально)

Мобильное приложение участника организации ЧЗОГ. Работает через API сайта
(`chzog.iniproject.ru`), данные — из общей базы. Доступ — по учётной записи и личному делу.

## 1. Первый запуск

1. **Заставка** — логотип ЧЗОГ, проверка сохранённого токена.
2. Если токена нет — **экран входа**: Логин, Пароль → «ВОЙТИ».
3. Если у учётной записи включена **2FA** — второй шаг: код из приложения-аутентификатора.
4. Токен сохраняется в защищённом хранилище (secure storage) — при следующих запусках вход не требуется.
5. Если аккаунт **не привязан к личному делу** — доступ к разделам ограничен, показывается профиль без должности (обратиться к администратору).

## 2. Главный экран

Сверху — **профиль**: логин, должность, дело, допуск, часовой пояс. Кнопка «Выйти» (справа).

Ниже — **плитки разделов**:
| Раздел | Назначение |
|---|---|
| ПРИКАЗЫ | документы, просмотр, подпись, PDF |
| ОБЪЕКТЫ | паспорта, зоны, карта |
| СМЕНЫ | моё расписание |
| ПРОХОДЫ | вход/выход, история |
| НОВОСТИ | лента и объявления |
| УВЕДОМЛЕНИЯ | что нового |
| АНКЕТЫ | заявки на службу (для кадровиков) |

Набор плиток зависит от прав (должности): рядовой видит меньше, руководство — больше.

## 3. Разделы — по шагам

### Приказы
1. Плитка «ПРИКАЗЫ» → список: №, наименование, дата, статус, гриф, метка PDF.
2. Тап по приказу → **карточка**: шапка вида («КАДРОВЫЙ ПРИКАЗ» и т.п.), реквизиты, текст, поля шаблона.
3. Блок **«Подписи»** — список подписантов со статусами (подписано / ожидает / отложено).
4. Если **моя подпись ожидает** — кнопки **«✍️ Подписать»** и **«🕓 Отложить»**.
5. Кнопка **«Открыть PDF»** — встроенный просмотр документа.
6. После подписания статус обновляется, а в канал/руководителю уходит уведомление (на стороне сайта).

### Объекты
1. Плитка «ОБЪЕКТЫ» → список объектов (название, тип, состояние, зоны).
2. Тап → **паспорт объекта**: тип, состояние, зоны, описание.
3. Кнопка **«Открыть на карте»** → карта **внутри приложения** (встроенный браузер), точка по координатам.
4. (план) **QR-метка** объекта — для чекина сканированием.

### Смены
1. Плитка «СМЕНЫ» → мой график: дата, день/ночная, пост, статус.
2. (план) Напоминания push за час до начала; начало: **дневная 12:00, ночная 00:00** в моём часовом поясе.

### Проходы
1. Плитка «ПРОХОДЫ» → поле «Объект» + кнопка **«ОТМЕТИТЬ»** (вход/выход чередуется автоматически).
2. Ниже — история проходов (вход/выход, объект, время).
3. (план) **QR-чекин**: сканирование QR на КПП вместо ввода объекта.

### Новости / Уведомления
- «НОВОСТИ» — лента объявлений (заголовок, текст, автор).
- «УВЕДОМЛЕНИЯ» — персональные/общие, кнопка «Отметить прочитанными».

### Анкеты (кадровики)
- Список заявок на службу, кнопки **«Принять» / «Отклонить»**.

## 4. Спец-сценарии

**Подписать приказ**
1. Приходит push «На подпись: № …».
2. Открываю приказ → читаю текст → «✍️ Подписать» (или «🕓 Отложить»).
3. Статус меняется; вижу «Подписи: N из M».

**Отметить проход на КПП**
1. «ПРОХОДЫ» → (план) «Сканировать QR» → камера → отметка.
2. Либо вручную: объект + «ОТМЕТИТЬ». Направление (вход/выход) определяется по последнему проходу.

**Рапорт об инциденте** (план, Этап 4)
1. «Рапорт» → текст → **фото с камеры** → **геолокация** автоматически.
2. Отправка → ССБ получает уведомление.

## 5. Офлайн (план, Этап 4)

- Данные (приказы, объекты, смены) кэшируются — доступны без связи.
- Действия (рапорт, проход) кладутся в **очередь** и уходят при появлении сети.

## 6. Настройки и безопасность

- **Часовой пояс** задаётся в кабинете сайта (влияет на время смен/напоминаний).
- **2FA** включается в кабинете сайта; в приложении — только ввод кода.
- **Выход** — кнопка в шапке главного экрана (токен удаляется).

## 7. Карта этапов

- ✅ Этап 1 — API авторизации (login + 2FA, токены).
- ✅ Этап 2 — вход и профиль.
- ✅ Этап 3 — разделы: приказы (подпись), объекты (карта), смены, проходы, новости, уведомления, анкеты.
- ⏳ Этап 4 — камера (фото инцидента, QR), GPS, push, офлайн.
- ⏳ Этап 5 — релиз (APK → Google Play; iOS — на Mac/TestFlight).
